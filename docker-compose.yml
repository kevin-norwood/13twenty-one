version: '3.9'

services:
  postgres:
    image: postgres:17.5
    container_name: summarizer-postgres
    restart: unless-stopped
    env_file:
      - .env.local
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: